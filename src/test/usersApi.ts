import Axios from 'axios';

import { store } from 'redux/reduxStore';

store = {
  userTypes: {
    admin: 1,
    justAUser: 2,
  },
};

const createUser = (name, userTypes, Types) => {
  store.getState().players;
  Axios.post('', { ajsdhfakjshdflka });
};
